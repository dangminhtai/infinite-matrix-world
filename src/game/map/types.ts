export type MapEnemy = {
  id: string;
  worldX: string;
  worldY: string;
  offsetX: number;
  offsetY: number;
};

export type TrackedTarget = MapEnemy;

export type MapWaypoint = {
  id: string;
  worldX: string;
  worldY: string;
  offsetX: number;
  offsetY: number;
};
